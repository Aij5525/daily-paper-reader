---
title: Beamforming Design Via GNN in mmWave Cell-Free Massive MIMO Using Sub-6 GHz CSI
title_zh: 基于GNN的毫米波无蜂窝大规模MIMO波束成形设计：利用Sub-6 GHz CSI
authors: "Sina Tavakolian, Abolfazl Zakeri, Ahmed Alkhateeb, Markku Juntti, Nhan Thanh Nguyen"
date: 2026-08-31
pdf: "https://arxiv.org/pdf/2608.30524v1"
tags: ["query:wireless-llm"]
score: 9.0
evidence: 利用子6GHz信道状态信息通过图神经网络学习毫米波无小区波束成形
tldr: 毫米波无蜂窝大规模MIMO波束成形依赖高开销的毫米波CSI获取。本文提出基于图神经网络（GNN）的方法，利用子6GHz CSI学习全数字波束成形，将系统建模为无线图，通过消息传递捕获用户间干扰与基站间协作。仿真显示该方法仅用子6GHz CSI即可达到或超过依赖完整毫米波CSI的经典方案，大幅降低训练开销并保持甚至提升和速率性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.30524v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 604, \"height\": 426, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30524v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 605, \"height\": 424, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.30524v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 602, \"height\": 428, \"label\": \"Figure\"}]"
motivation: 毫米波CFmMIMO波束成形需准确CSI但训练开销大，利用子6GHz CSI可降低开销。
method: 将CFmMIMO系统表示为无线图，用GNN通过消息传递机制从子6GHz CSI学习近似最大化下行和速率的波束成形器。
result: 仿真表明所提方法仅用子6GHz CSI即达到或超过依赖全毫米波CSI的经典基线，和速率性能更优。
conclusion: 子6GHz CSI辅助的GNN波束成形可有效替代毫米波CSI，降低开销且保持高性能。
---

## 摘要
毫米波（mmWave）无蜂窝大规模多输入多输出（CFmMIMO）系统中的波束成形方法需要准确的信道状态信息（CSI），而获取这些信息需要大量的训练开销。本文表明，利用图神经网络（GNN）可以从sub-6 GHz CSI中有效学习全数字化无蜂窝毫米波波束成形。具体而言，我们将CFmMIMO系统表示为无线图，并训练GNN基于可用的sub-6 GHz CSI来近似最大化下行链路和速率的波束成形器。提出了一种消息传递机制，以捕获不同网络拓扑中的用户间干扰和基站间合作。仿真结果表明，与依赖完整毫米波CSI的经典基线方法相比，所提出的基于sub-6 GHz辅助GNN的波束成形器在可实现和速率方面具有竞争力，且通常表现更优。

## Abstract
Beamforming methods in millimeter-wave (mmWave) cell-free massive multiple-input multiple-output (CFmMIMO) systems require accurate channel state information (CSI), whose acquisition entails significant training overhead. This paper shows that fully digital cell-free mmWave beamforming can be effectively learned from sub-6 GHz CSI using a graph neural network (GNN). Specifically, we represent a CFmMIMO system as a wireless graph, and the GNN is trained to approximate beamformers that maximize the downlink sum-rate based on the available sub-6 GHz CSI. A message-passing mechanism is proposed to capture inter-user interference and inter-base-station cooperation across different network topologies. Simulation results demonstrate that the proposed sub-6 GHz-assisted GNN-based beamformer achieves competitive and often superior sum-rate performance compared to classical baselines that rely on full mmWave CSI.

---

## 论文详细总结（自动生成）

# 论文总结：基于GNN的毫米波无蜂窝大规模MIMO波束成形设计（利用Sub-6 GHz CSI）

## 1. 核心问题与整体含义

- **研究背景**：无蜂窝大规模MIMO（CFmMIMO）是未来无线网络的重要架构，多个分布式基站（BS）联合服务用户（UE）。当工作在毫米波（mmWave）频段时，波束成形是应对路径损耗和利用大规模天线阵列的关键，但传统方案依赖准确的信道状态信息（CSI），而 mmWave CSI 的获取需要大量波束训练和反馈开销。
- **核心问题**：能否**仅利用 sub-6 GHz 频段的上行 CSI**（估计开销低、易获取）来学习 mmWave 下行全数字波束成形，从而避免高开销的 mmWave CSI 获取过程？
- **整体含义**：论文给出了肯定答案——提出一种基于图神经网络（GNN）的端到端学习框架，将 CFmMIMO 系统建模为“无线图”，直接从 sub-6 GHz CSI 学习可最大化下行和速率的数字波束成形矩阵。该框架在仅使用 sub-6 GHz CSI 的条件下，性能接近甚至优于依赖完整 mmWave CSI 的经典方案，同时大幅降低了训练/反馈开销。

## 2. 方法论

- **问题建模**：
  - 系统包含 B 个 BS、U 个 UE；每个 BS 配备 sub-6 GHz 阵列（N_s 天线）和 mmWave 阵列（N_mW 天线），UE 单天线。
  - 下行和速率 `R_sum = Σ_u log2(1 + SINR_u)`，其中 SINR 由所有 BS 到 UE 的 beamforming 向量共同决定。
  - 目标：在每 BS 功率约束下最大化 `R_sum`，但 mmWave CSI 不可用，只有 uplink sub-6 GHz CSI `H_s` 可用。于是学习映射 `Φ_s(H_s) = {F_b}`。
- **图构建**：
  - 节点：BS 节点和 UE 节点；边：每条 BS–UE 链路对应一条边。
  - 边特征来自 sub-6 GHz 信道向量 `h_s_{b,u}`；每个节点关联一个可学习嵌入向量（`ρ_b`, `ρ_u`）。
- **GNN 架构（三阶段）**：
  1. **初始化（Phase 1）**：节点状态设为可学习嵌入；边状态由线性映射 `φ_init_edge(h_s_{b,u})` 得到，映射到 d 维。
  2. **消息传递（Phase 2）**：堆叠 T 层（本文 T=3）消息传递层。对每条边 `(b,u)`：
     - BS 侧邻居：与同一 BS b 相连的其他边；UE 侧邻居：与同一 UE u 相连的其他边。
     - 分别用注意力机制（类似 EdgeGATConv）计算注意力系数（BS 侧 α，UE 侧 β），再聚合邻居边的消息，得到 `m^BS_{b,u}` 和 `m^UE_{b,u}`。
     - 融合两方向消息：`m_{b,u} = 0.5 * (m^BS + m^UE)`。
     - 残差更新边状态：`e_new = W_upd * e_old + m`。
  3. **读出（Phase 3）**：对每条边构造上下文向量 `c_{b,u} = concat(e_T, c_intra, c_inter)`，其中 `c_intra` 是同一 BS 侧邻居边特征的均值，`c_inter` 是同一 UE 侧邻居边特征的均值；再通过 MLP `φ_out` 生成波束向量 `f_{b,u}`；最后按每 BS 功率约束归一化形成数字波束成型矩阵 `F_b`。
- **训练方式**：
  - **自监督/端到端**：损失函数直接设为负的下行和速率 `L = -R_sum`，不需要监督标签（如最优波束向量）。
  - 离线训练时需要 mmWave CSI 来计算损失；**在线推理时仅需 sub-6 GHz CSI** 通过一次前向传播得到波束成形矩阵。
  - 文中给出了完整的训练流程（Algorithm 1），包括数据集构造、参数初始化、消息传递、读出、损失计算和参数更新。

## 3. 实验设计

- **数据集与场景**：
  - 使用 **DeepMIMO 数据集** 的 O1 场景，基于射线追踪（Remcom Wireless InSite）生成信道。
  - 频段配置：O1_28（28 GHz mmWave）和 O1_3p5（3.5 GHz sub-6 GHz），保证跨频段空间一致性。
- **系统参数**：
  - B = 3 个 BS；每个 BS 的 mmWave 阵列 N_mW = 32 天线，sub-6 GHz 阵列 N_s = 8 天线。
  - 带宽：mmWave 0.1 GHz、sub-6 GHz 0.01 GHz；传播路径数 L = 15。
- **数据构建**：
  - 每个图中随机选择 UE 数量（范围 [3, 8]），所有 UE 与所有 BS 连接，形成完全二分图。
  - 共生成 10,000 个图，按 80%/20% 划分为训练/测试集。
- **模型与训练超参数**：
  - GNN：T=3 层消息传递，隐藏维度 d=64；LeakyReLU 斜率 0.2；读出 MLP 3 层（ReLU）。
  - 优化器：Adam，学习率 1e-4，权重衰减 1e-5；批大小 10；训练 15 epochs。
- **对比方法（Baselines）**：
  - 使用完整 mmWave CSI 的经典方案：**ZF（迫零）、MMSE、MRT（最大比发送）**。
  - 另加入 **MMSE（均匀功率分配）** 作为消融变量，用于评估 GNN 学习到的功率分配带来的增益。
  - 为确保公平，所有方法使用相同的 GNN 诱导的每 UE 功率分配（即各方法只决定波束方向，功率分配由 GNN 产生）。

## 4. 资源与算力

- **论文未明确说明使用的 GPU 型号、数量或具体训练时长**。仅给出训练轮数（15 epochs）、批大小等超参数，未提供硬件资源和运行时间信息。

## 5. 实验数量与充分性

- **主要实验组数**：
  - 图1：训练收敛性分析（Pb = 1 W 和 3 W 两种情况）。
  - 图2：测试时可用的 sub-6 GHz 天线数量（3~8）对和速率的影响。
  - 图3：和速率 vs. 每 BS 发射功率 Pb（1~9 W），对比 ZF、MMSE、MMSE(Uniform)、MRT。
- **充分性评估**：
  - 优点：涵盖了收敛性、鲁棒性（部分 CSI）、综合对比（多种经典方法）、消融（功率分配贡献），且验证了不同 UE 数量下的泛化能力。
  - 不足：仅使用了单一数据集场景（O1）、固定 BS 数（3）、固定天线

补全上文第 5 节末尾：

- **不足**：仅使用了单一数据集场景（O1）、固定 BS 数（3）、固定天线配置（sub-6 GHz 为 8 天线、mmWave 为 32 天线），缺乏对多 BS 数、不同天线规模、不同小区布局的泛化验证；也未与基于 sub-6 GHz CSI 的传统映射/插值方法（如子空间投影或稀疏重构）进行对比；消融实验仅针对“功率分配”这一因素，未深入分析 GNN 结构（如层数、注意力机制）的影响。

## 6. 结果与主要发现

- **训练收敛性（图 1）**：在 Pb = 1 W 和 3 W 两种功率设置下，训练损失（负和速率）随 epoch 稳定下降并趋于收敛，说明端到端训练是有效的。
- **sub-6 GHz 天线数影响（图 2）**：测试时仅使用部分 sub-6 GHz 天线（3~8 根）也能获得不错性能；随着可用天线数量增加，和速率逐渐提升。这说明 GNN 能利用有限且不完整的 sub-6 GHz CSI 提取有用空间信息。
- **和速率 vs. 发射功率（图 3）**：在不同 Pb 下，GNN 方法（仅 sub-6 GHz CSI）的性能始终优于使用完整 mmWave CSI 的 MRT，且接近或略优于 ZF 和 MMSE（二者依赖完整 mmWave CSI）。在较高功率区，GNN 的增益更加明显。
- **功率分配增益**：与“MMSE（均匀功率分配）”相比，GNN 自动学到的功率分配带来了明显的和速率增益，证明端到端学习不仅能确定波束方向，还能优化功率分配。
- **跨 UE 数量的泛化**：模型在 UE 数量为 3~8 的随机图中训练，测试时能适应不同 UE 数量，说明 GNN 具备对图规模的归纳能力。

## 7. 结论

- 论文提出了一种仅利用 sub-6 GHz 上行 CSI 的 GNN 框架，用于无蜂窝大规模 MIMO 的 mmWave 下行数字波束成形设计。
- 通过将系统建模为“无线图”并在边状态上进行消息传递，GNN 能够学习到 BS–UE 间的高层空间关系，从而直接生成波束成形矩阵。
- 实验表明，该框架在避免 mmWave CSI 获取开销的前提下，性能可达到甚至超越依赖理想 mmWave CSI 的经典方案，为跨频段 CSI 辅助的波束成形提供了新思路。

## 8. 局限性与未来方向

- **场景单一**：仅验证了单个射线追踪场景，未来需要在更多信道环境（如城市峡谷、室内、车联网）中测试。
- **频段假设**：假设 sub-6 GHz 与 mmWave 之间存在空间一致性，实际部署中可能受天线位置、校准误差影响，需要更鲁棒的设计。
- **CSI 噪声与时效性**：实验未考虑 sub-6 GHz CSI 的估计误差、反馈延迟等非理想因素，实际系统中需加入噪声鲁棒性训练。
- **扩展方向**：可考虑联合优化导频设计、混合波束成形（模拟+数字）、用户调度等；也可将方法扩展到可重构智能表面（RIS）辅助的系统中。

## 9. 总结评价

- 论文的贡献在于用 GNN 实现了“跨频段 CSI 迁移”式的波束成形，显著降低了 mmWave 接入开销，且方法具有较好的可解释性（节点/边对应物理实体）。
- 自监督的端到端训练方式避免了对最优波束标签的依赖，使框架易于推广。
- 实验对比公平且包含必要消融，但验证范围有限、缺少与传统 sub-6 GHz 到 mmWave 映射方法的对比，是其主要弱点。
- 整体而言，该工作为超高频段波束管理提供了新颖且可行的机器学习方案，具有较强的实际参考价值。

（完）
