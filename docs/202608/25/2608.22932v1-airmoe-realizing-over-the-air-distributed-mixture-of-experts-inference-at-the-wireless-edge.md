---
title: "AirMoE: Realizing Over-the-Air Distributed Mixture-of-Experts Inference at the Wireless Edge"
title_zh: AirMoE：在无线边缘实现空中分布式混合专家推理
authors: "Huiling Yang, Zhanwei Wang, Kaibin Huang"
date: 2026-08-24
pdf: "https://arxiv.org/pdf/2608.22932v1"
tags: ["query:wireless-llm"]
score: 9.0
evidence: 利用空中计算实现无线边缘大模型推理
tldr: 无线边缘部署MoE大模型面临上行瓶颈，AirMoE利用空中计算实现专家输出叠加聚合，通过构建推理感知误差度量、分层优化功率控制与专家放置，在OLMoE模型上显著提升端到端推理精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.22932v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 653, \"height\": 555, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 732, \"height\": 748, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 790, \"height\": 394, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 613, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 636, \"height\": 400, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1690, \"height\": 639, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1831, \"height\": 477, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.22932v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 869, \"height\": 371, \"label\": \"Figure\"}]"
motivation: WIDE架构中专家输出经正交多址重复上传造成严重上行瓶颈，需利用AirComp实现高效聚合。
method: 构造推理感知误差度量，分解为双时间尺度优化：快速功率控制与慢速信道感知专家放置。
result: 在ARC-Easy上，AirMoE显著优于基线，尤其在设备异构性强时精度提升明显。
conclusion: AirMoE为无线分布式MoE推理提供有效框架，验证了空中计算在边缘AI中的潜力。
---

## 摘要
混合专家（MoE）架构通过稀疏专家激活减少每令牌计算量，从而在无线边缘实现高效的大语言模型（LLM）推理。无线分布式MoE（WIDE）架构通过将计算密集型的专家分布在由边缘服务器协调的设备上，解决了边缘设备的资源约束问题。然而，在正交多址接入上重复上传高维专家输出造成了严重的上行链路瓶颈。为克服这一限制，我们提出了AirMoE，一种利用空中计算（AirComp）通过无线波形叠加实现专家输出同时聚合的新框架。然而，将AirComp集成到MoE推理中带来了三个独特挑战：快速变化的聚合权重、与层相关的误差敏感性以及信道感知的专家放置。为应对这些挑战，我们首先构建了一个推理感知的AirMoE误差度量，通过基于扰动的层敏感性校准，量化物理层聚合失真对端到端（E2E）推理精度的影响。随后，我们制定了一个联合优化问题以最小化该E2E误差，并在不损失最优性的前提下将其分解为双时间尺度框架。在快时间尺度上，我们推导了一个全局最优的基于阈值的功率控制策略，将设备分为两组：一组实现精确的聚合权重对齐，其余以最大功率传输。在慢时间尺度上，我们开发了一种激活与信道感知的专家放置策略，将更重要的专家分配给信道功率成本更低的设备。使用OLMoE-1B-7B-0924模型在ARC-Easy基准上的大量实验表明，AirMoE在端到端推理精度上显著优于代表性基线，尤其是在设备异构性较强的情况下。

## Abstract
Mixture-of-experts (MoE) architectures enable efficient large language model (LLM) inference at the wireless edge by reducing per-token computation through sparse expert activation. The wireless distributed MoE (WIDE) architecture addresses edge-device resource constraints by distributing computation-intensive experts across devices coordinated by an edge server. However, repeated uploads of high-dimensional expert outputs over orthogonal multiple access create a severe uplink bottleneck. To overcome this limitation, we propose AirMoE, a novel framework leveraging over-the-air computing (AirComp) to realize simultaneous expert-output aggregation via wireless waveform superposition. Integrating AirComp into MoE inference, however, introduces three unique challenges: fast-varying aggregation weights, layer-dependent error sensitivity, and channel-aware expert placement. To address these challenges, we first construct an inference-aware AirMoE error metric to quantify the effect of physical-layer aggregation distortion on end-to-end (E2E) inference accuracy via perturbation-based layer-sensitivity calibration. We then formulate a joint optimization problem to minimize this E2E error and decompose it, without loss of optimality, into a two-timescale framework. At the fast timescale, we derive a globally optimal threshold-based power-control policy that partitions devices into two groups: those achieving exact aggregation-weight alignment, and the remainder transmitting at maximum power. At the slow timescale, we develop an activation- and channel-aware expert placement strategy that assigns more important experts to devices with lower channel-power cost. Extensive experiments using the OLMoE-1B-7B-0924 model on the ARC-Easy benchmark demonstrate that AirMoE significantly outperforms representative baselines in E2E inference accuracy, particularly under strong device heterogeneity.

---

## 论文详细总结（自动生成）

# AirMoE 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：6G 移动网络将支持在无线边缘部署大语言模型（LLM），为低时延智能应用（如 VR、自动驾驶、个性化助手）提供推理能力。然而，LLM 的计算与存储需求远超单个边缘节点的能力。
- **MoE 架构的契机**：混合专家（Mixture-of-Experts, MoE）通过稀疏专家激活显著降低每令牌计算量，同时保持大模型容量，被视为边缘部署的理想架构。
- **WIDE 架构及其瓶颈**：无线分布式 MoE（WIDE）将计算密集的专家子网络分布到边缘设备上，由边缘服务器协调执行。但推理中需要反复将专家输出从设备上传至服务器，现有的正交多址接入方式导致严重上行链路瓶颈。
- **AirMoE 的核心思想**：利用空中计算（AirComp）的波形叠加特性，使多个设备的专家输出在同一无线信道上同时传输并直接在接收端完成加权聚合，从而消除上行瓶颈。
- **挑战**：将 AirComp 集成到 MoE 推理中面临三个独特挑战——快速变化的 gating 权重（令牌级别）、层相关的误差敏感性（不同层对失真的容忍度不同）、以及信道感知的专家放置（专家与设备的映射关系直接影响聚合精度）。

## 2. 方法论

### 核心思想
将 MoE 层中反复执行的 gating 加权专家聚合（o = Σ gᵢ·vᵢ）天然映射到无线信道的波形叠加，同时通过联合优化功率控制与专家放置，最小化端到端（E2E）推理精度损失。

### 关键技术细节与公式

**（1）推理感知的 AirMoE 误差度量**

- 定义层间 AirMoE 误差为重建聚合输出与理想聚合输出的归一化 MSE：
  - MSE_ℓ = c²_ℓ · [ Σ_{m∈A} (|h_m|√p_m/η − g_m)² + σ²/η² ]
  - 第一项表示有效无线聚合系数与 gating 权重之间的系数失配，第二项为去噪后的有效噪声。
- 通过**扰动实验**离线校准各层的敏感性系数 {aℓ}：在每层注入不同强度的高斯扰动，测量精度下降程度，定义敏感性系数为达到目标精度下降（如 ρ=3%）所需最小扰动强度的倒数。

**（2）双时间尺度优化分解**

- 定义整体 AirMoE 误差为层敏感性加权长期聚合失真：E_A = E[Σ aℓ·MSE_ℓ^(t)]
- 提出联合优化问题（最小化 E_A），并利用两时间尺度结构将其**无损分解**：
  - **快时间尺度**（瞬时功率控制）问题 (24)：对每次聚合，优化设备发射功率与服务器去噪因子。
  - **慢时间尺度**（专家放置）问题 (25)：基于最优功率控制下获得的误差，优化专家到设备的长期映射。

**（3）最优功率控制策略（Theorem 1）**

- 对于固定 η，每个设备的最优发射功率为闭式解：p̃_m(η) = min { (η·g_m/|h_m|)², P_m }——若能对齐则精确对齐，否则满功率发射。
- 定义饱和阈值 τ_m = |h_m|√P_m / g_m，将设备分为"对齐区"与"饱和区"。
- 全局最优策略为：在排序后的阈值区间内，通过枚举 R_n 区间并求解各区间的平滑子问题（Lemma 2 给出闭式驻点），选出最小误差对应的 η* 与 p*。
- **渐近分析**：高 SNR 下退化为 gating 加权信道反演；低 SNR 下退化为满功率发射。

**（4）激活与信道感知的专家放置策略（Theorem 2）**

- 通过上界导出一个**可分离的优化目标**，因子化为专家重要性 W_{ℓ,i} 与设备成本 R_m 的乘积：
  - W_{ℓ,i} = a_ℓ · c²_ℓ · Pr(激活) · E[g² | 激活]，综合层敏感性、输出尺度、激活频率与 gating 权重。
  - R_m = E[1/(|h_m|²P_m)]，表征设备长期逆信道-功率能力。
- **秩保持放置规则**：将专家按重要性降序排列、设备按成本升序排列，依次匹配。即最重要的专家分配给信道条件最好的设备。

## 3. 实验设计

- **数据集/基准**：ARC-Easy（标准英文多项选择问答基准）上的语言推理任务，使用 lm-evaluation-harness 工具包。离线统计（归一化参数、层敏感性、专家重要性权重）由小型校准集估计。
- **模型**：OLMoE-1B-7B-0924，含 16 个 MoE 层，每层 64 个专家，Top-8 路由策略。
- **无线设置**：块衰落信道，大尺度衰落模型（d⁻²·⁸ 路径损耗 + 对数正态阴影 σ_sh），网络平均接收 SNR 按定义变化，设备发射功率预算统一 0.2 W。
- **对比方法**：在功率控制维度对比 FullPower、ChannelInv、TruncInv；在专家放置维度对比 ActFreqPlace、GatePlace、RandPlace、LayerUnaware。所有基线均在相同 AirMoE 框架下仅改变所评测组件。

## 4. 资源与算力

- 论文**未明确报告** GPU 型号、GPU 数量、训练/微调时长或具体的计算资源开销。
- 仅可推断：使用了 OLMoE-1B-7B-0924 这一开源 MoE 模型，其规模约 1B 激活参数/7B 总参数，推理实验（非训练）以及离线校准实验的计算负担相对可控，但具体硬件配置未披露。

## 5. 实验数量与充分性

论文进行了**四组主要实验**：

1. **功率控制消融**（图 6a）：不同 SNR 下对比三种功率控制基线。
2. **专家放置消融**（图 6b）：不同 SNR 下对比四种放置基线。
3. **层敏感性消融**（图 7）：对比 LayerUnaware 与加权误差度量的有效性。
4. **设备异构性分析**（图 8）：不同阴影衰落标准差 σ_sh 下的性能对比。

**评价**：
- **充分性**：覆盖了方法论的各个核心环节，消融设计清晰，分别验证了功率控制、放置策略、层敏感性校准、异构性鲁棒性四个方面的贡献。
- **公平性**：所有基线共享同一框架、只替换被评测组件，对比较为公平。
- **局限**：仅在一个数据集（ARC-Easy）和一个模型（OLMoE-1B-7B-0924）上验证，缺乏多数据集、多模型（如 Qwen3-MoE、DeepSeek-V3）及真实硬件原型验证的支撑，结论的外部效度有限。

## 6. 主要结论与发现

- AirMoE 显著优于所有代表性基线，尤其在**低/中 SNR 区域**与**强设备异构性**下优势明显。
- 最优功率控制在极低 SNR 退化为满功率发射、极高 SNR 退化为信道反演，验证了阈值策略的理论渐近分析。
- 层敏感性加权误差度量比未加权误差更贴近实际推理精度变化趋势，是更有效的任务相关失真指标。
- 设备异构性越强，秩保持的专家放置策略带来的增益越大；随机放置在高异构性下显著退化。
- **总体结论**：无线分布式推理应当利用神经网络执行的结构特性，而非将通信视为独立传输层；聚合失真应按照其对推理性能的层依赖效应进行评估，且应优先将关键专家分配给信道能力强的设备。

## 7. 优点

- **问题定位精准**：直击 WIDE 架构的上行瓶颈，将 MoE 聚合与 AirComp 的数学结构自然对齐，动机充分且令人信服。
- **理论深度充足**：提供了完整的双时间尺度分解证明、闭式最优功率控制策略（含逐区间求解）、秩保持最优放置定理其交换证明，理论体系严密。
- **时间尺度分离巧妙**：利用功率控制（瞬时）与放置（长期）的天然时间尺度差异实现无损分解，极大降低求解复杂度。
- **误差度量任务相关**：提出层敏感性校准的推理感知误差度量，而非传统物理层 MSE，是对 AirComp 设计中"任务导向"理念的良好实践。
- **放置规则简单可解释**：专家重要性×设备成本的因子化分解带来简洁的秩匹配规则，便于实际落地。
- **实验消融完整**：从功率控制、放置、层敏感性到异构性，系统地验证了每个技术组件的必要性。

## 8. 不足与局限

- **实验覆盖单一**：仅使用 ARC-Easy 数据集与 OLMoE-1B-7B-0924 模型，未覆盖其他常见推理基准（如 MMLU、GSM8K、HumanEval）或更大规模/不同类型的 MoE 模型，泛化性结论需要进一步验证。
- **理想化假设**：假设完美的 CSI 可用、符号级同步（依赖 timing advance）、下行传输无失真、通道块衰落恒定——这些假设在真实系统中往往难以完全满足。
- **相关性问题**：MSE 推导假设不同专家的归一化符号不相关；实际中专家输出可能存在相关性，论文仅以 Cauchy 不等式给出上界，但该上界在实践中的紧密程度未量化。
- **应用限制**：未建模专家迁移成本、设备移动性、干扰环境及多小区场景；多专家每设备扩展仅建议用整数线性规划求解，未给出算法实现或仿真验证。
- **资源信息缺失**：未报告实验的具体硬件资源与能耗，难以评估方案在实际部署中的性价比。

（完）
